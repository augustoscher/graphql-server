
import movieResolver from './movieResolver'
import serieResolver from './serieResolver'
import postResolver from './postResolver'

const resolvers = [movieResolver, serieResolver, postResolver]

export default resolvers
